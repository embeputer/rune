# Optional — `bun run apps/gateway/src/cli.ts login` writes ~/.rune/config.json
# instead. Use this file only for headless / CI setups where you want to skip
# interactive login.
#
# Copy to `.env` (gitignored).

RUNE_SUPABASE_URL=
RUNE_SUPABASE_ANON_KEY=
RUNE_WORKSPACE_ROOT=
